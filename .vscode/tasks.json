{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Register user",
            "type": "shell",
            "command": "python3 client_app\\server.py"
        },
        {
            "label": "Start DB",
            "type": "shell",
            "command": "python3 database_handler.py",
            "options": {
                "cwd": "${workspaceFolder}\\database"
            }
        }
    ]
}